import React from 'react';
import HexagonLayout from '../Layout/HexagonLayout';

const SkillBloc = ({ name, number }) => (
  <HexagonLayout><span>{name}</span></HexagonLayout>
);

export default SkillBloc;
